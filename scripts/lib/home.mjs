/**
 * La maison (Sotteville-lès-Rouen) : point de départ/retour de plusieurs
 * éditions. Constante unique — les edition.json y font référence avec la
 * valeur "home" dans transfers.from / transfers.to, et gen-background.mjs
 * l'utilise pour le marqueur du filigrane.
 */
export const HOME = [1.09281, 49.4239]; // [lon, lat]
